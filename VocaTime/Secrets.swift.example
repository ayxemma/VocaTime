import Foundation

/// Copy this file to `Secrets.swift` in the same folder (same target), paste your key, and build.
/// `Secrets.swift` is gitignored and will not be committed.
enum Secrets {
    static let openAIAPIKey = "YOUR_API_KEY_HERE"
}
